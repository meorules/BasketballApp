<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Forms;assembly=SkiaSharp.Views.Forms"
             xmlns:tt="clr-namespace:TouchTracking.Forms;assembly=TouchTracking.Forms"
             x:Class="BasketballApp.Views.DataCollectionPage">
  <ContentPage.Content>
    <AbsoluteLayout x:Name="myabs">
      <BoxView Color="Navy" CornerRadius="10" AbsoluteLayout.LayoutBounds="400,500,1000,1000"/>
      <skia:SKCanvasView  
                   AbsoluteLayout.LayoutBounds="0,0,1,1"  
                    AbsoluteLayout.LayoutFlags="SizeProportional"  
                     
                   x:Name="canvasView" PaintSurface="OnCanvasViewPaintSurface">
        <skia:SKCanvasView.Effects>
          <tt:TouchEffect Capture="True"  TouchAction="TouchEffect_TouchAction" />
        </skia:SKCanvasView.Effects>
      </skia:SKCanvasView>


      <BoxView Color="PowderBlue" CornerRadius="10" AbsoluteLayout.LayoutBounds="405,5,400,60"/>
      <Button Command="{Binding LoginCommand}" CornerRadius="10" BackgroundColor="Black" x:Name="Timer" Padding="2" FontSize="Title" Text="09:36" TextColor="Red" AbsoluteLayout.LayoutBounds="510,17,70,40"/>
      <Button Command="{Binding LoginCommand}" CornerRadius="10" BackgroundColor="Black" x:Name="ShotClock" Padding="2" FontSize="Title" Text="24.0" TextColor="Green" AbsoluteLayout.LayoutBounds="585,17,55,40"/>
      <Button Command="{Binding LoginCommand}" CornerRadius="10" BackgroundColor="Black" x:Name="Quarter" Padding="2" FontSize="Title" Text="Q1" TextColor="White" AbsoluteLayout.LayoutBounds="645,17,55,40"/>
      <Button Style="{StaticResource ClockButtonStyle}" FontFamily="Material" Text="&#xe037;" Command="{Binding LoginCommand}" AbsoluteLayout.LayoutBounds="410,15,40,40"/>
      <Button Style="{StaticResource ClockButtonStyle}" FontFamily="Material" Text="&#xe034;" Command="{Binding LoginCommand}" AbsoluteLayout.LayoutBounds="460,15,40,40"/>
      <Button Style="{StaticResource ClockButtonStyle}" FontFamily="Material" Text="&#xe047;" Command="{Binding LoginCommand}" AbsoluteLayout.LayoutBounds="705,15,40,40"/>
      <Button Style="{StaticResource ClockButtonStyle}" Padding ="0" Text="T.O" Command="{Binding LoginCommand}" AbsoluteLayout.LayoutBounds="750,15,40,40"/>

      <BoxView Color="Black" CornerRadius="10" AbsoluteLayout.LayoutBounds="2,275,400,62"/>
      <Label BackgroundColor="DarkRed" x:Name="HomeTeamName" Padding="2" FontSize="Title" Text="Home" TextColor="White" AbsoluteLayout.LayoutBounds="8,290,70,40"/>
      <Label BackgroundColor="Transparent" x:Name="HomeFouls" Padding="0" FontSize="50" Text="-------" TextColor="White" AbsoluteLayout.LayoutBounds="20,250,100,40"/>
      <Label BackgroundColor="White" x:Name="HomeTeamScore" Padding="4" FontSize="Title" Text="128" TextColor="Black" AbsoluteLayout.LayoutBounds="78,290,50,40"/>
      
      <Label BackgroundColor="Navy" x:Name="AwayTeamName" Padding="3" FontSize="Title" Text="Away" TextColor="White" AbsoluteLayout.LayoutBounds="130,290,70,40"/>
      <Label BackgroundColor="Transparent" x:Name="AwayFouls" Padding="0" FontSize="50" Text="-------" TextColor="White" AbsoluteLayout.LayoutBounds="140,250,100,40"/>
      <Label BackgroundColor="White" x:Name="AwayTeamScore" Padding="4" FontSize="Title" Text="135" TextColor="Black" AbsoluteLayout.LayoutBounds="200,290,50,40"/>
      
      <Button BackgroundColor="Green" Style="{StaticResource AwayButtons}" Text="+" Command="{Binding LoginCommand}" AbsoluteLayout.LayoutBounds="260,290,40,40"/>
      <Button BackgroundColor="Red" Style="{StaticResource AwayButtons}" Text="-" Command="{Binding LoginCommand}" AbsoluteLayout.LayoutBounds="305,290,40,40"/>
      <Button BackgroundColor="Navy" Style="{StaticResource AwayButtons}" Text="PF" Command="{Binding LoginCommand}" AbsoluteLayout.LayoutBounds="350,290,40,40"/>


    </AbsoluteLayout>

      <!--<

      <StackLayout>
        <ImageButton WidthRequest="50" HeightRequest="50" BackgroundColor="Transparent" Source="hamburger.png" Clicked="ImageButton_Clicked" ></ImageButton>
        <Frame IsVisible="false" x:Name="myframe"  WidthRequest="100" HeightRequest="70">
          <StackLayout>
            <Label FontSize="Small" Text="Edit"/>
            <Label FontSize="Small" Text="Duplicate"/>
            <Label FontSize="Small" Text="Remove"/>
          </StackLayout>
        </Frame>
      </StackLayout>

    -->
    
  </ContentPage.Content>
</ContentPage>