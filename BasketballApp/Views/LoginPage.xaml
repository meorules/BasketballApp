<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BasketballApp.Views.LoginPage"
             Shell.NavBarIsVisible="False"
             >
  <ContentPage.Content>
    <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
      <Label TextColor="White" HorizontalTextAlignment="Center" BackgroundColor="{StaticResource Primary}" Text="Welcome to The Basketball Data Collection App!" Padding="20" FontSize="20" />
      <Label Text="Username" TextColor="Black" HorizontalOptions="Center"/>
      <Entry VerticalOptions="Center" Text="{Binding username}" TextColor="Black" BackgroundColor="LightGray"/>
      <Label Text="Password" TextColor="Black" HorizontalOptions="Center"/>
      <Entry IsPassword="True" VerticalOptions="Center" Text="{Binding password}" TextColor="Black" BackgroundColor="LightGray"/>
      <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
      <Button VerticalOptions="Center" Text="Register" Command="{Binding RegisterCommand}"/>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>